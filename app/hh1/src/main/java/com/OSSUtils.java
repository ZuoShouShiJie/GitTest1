package com;

import com.aliyun.oss.OSSClient;

public class OSSUtils {/*
	private static OSSClient client = null;
	private static final String ACCESS_ID = ResourceUtil.getAccessKeyId();
	private static final String ACCESS_KEY = ResourceUtil.getAccessKeySecret();
	private static final String ENDPOINT = ResourceUtil.getCdnDomain();

	public static OSSClient getOSSClient() {
		if (client == null) {
			synchronized (OSSClient.class) {
				if (client == null) {
					client = new OSSClient(ENDPOINT, ACCESS_ID, ACCESS_KEY);
				}
			}
		}
		return client;
	}*/
}
